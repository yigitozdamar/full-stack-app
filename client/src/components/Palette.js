import React from "react";

const Palette = () => {
  return (
    <div className="Palette">
      <input type="color" name="color" />
      <button>Click</button>
    </div>
  );
};

export default Palette;
